import * as React from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
interface INewRecipeProps {
  
}

const RecipeNew: React.FunctionComponent<INewRecipeProps> = () => {
  return (
    <div>
      Create Recipe
    </div>
  );
}

export default RecipeNew;